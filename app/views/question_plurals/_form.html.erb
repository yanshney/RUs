<%= form_with(model: question_plural, local: true) do |form| %>
  <% if question_plural.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_plural.errors.count, "error") %> prohibited this question_plural from being saved:</h2>

      <ul>
      <% question_plural.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_field :question, id: :question_plural_question %>
  </div>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_field :answer, id: :question_plural_answer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
